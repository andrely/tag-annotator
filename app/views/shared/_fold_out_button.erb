<!-- Partial generating +/- toggling button for showing/hiding page elements.

     ERB render call ust proved the HTML id and text of the button and the element to toggle
     as local variables:

      - Button ID: button_id
      - Button text: button_text
      - Toggled element id: fold_out_elt_id
      - Initial state of button: initial_state
        TRUE displays '+', FALSE displays '-' -->

<!-- TODO generate button_id internally in the partial -->

<%
   # Default values
   button_text = "" if local_assigns[:button_text].nil?
   initial_state = TRUE if local_assigns[:initial_state].nil?
%>

<%= link_to(initial_state ? "+" : "-", "#",
            :id => button_id,
            :onclick => "toggleEltFromButton(#{fold_out_elt_id}, #{button_id});",
            :style => "text-decoration:none") %>

<%= button_text %>